# DNS and IPConfig Basics

## Date

May 3, 2026

## What I learned today

Today I learned about `ipconfig`, DNS cache, and DNS flushing.

This lab helped me understand how a computer checks its network information and how DNS records can be cleared when there are connection or name resolution issues.

## Key commands learned

### `ipconfig`

The `ipconfig` command is used in Windows Command Prompt to show basic network information.

It can show information like:

- IP address
- Subnet mask
- Default gateway
- DNS-related network settings

Example:

```cmd
ipconfig
